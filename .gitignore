# Python
__pycache__/
*.pyc
# scratch/temp
_*.js
_*.py
/tmp/
scratchpad/
.claude/
# local build toolchain (JDK + Android SDK) — do NOT commit
tools/
# Android project + build output (regenerable via bubblewrap)
android/
*.apk
*.aab
# signing secrets — NEVER commit
*.keystore
*.jks
keystore-password.txt
*.idsig
